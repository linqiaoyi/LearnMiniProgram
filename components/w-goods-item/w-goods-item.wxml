<!--components/w-goods-item/w-goods-item.wxml-->
<view class="goods-item" bind:tap="handlePushDetail" data-iid="{{item.iid}}">
    <image class="image" src="{{item.show.img || item.image ||item.img}}" mode="widthFix"/>
    <view class="desc-info">
      <view class="title">{{item.title}}</view>
      <view class="info">
        <text class="price">￥{{item.price}}</text>
        <image class="icon" src="/assets/common/favor.png"></image>
        <text class="cfav">{{item.cfav}}</text>
      </view>
      
    </view>
    
</view>
